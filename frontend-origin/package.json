{
  "name": "rustwebui-frontend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "rspack serve --config rspack.config.dev.ts",
    "build": "node ../build-frontend.js",
    "build:incremental": "rspack build --config rspack.config.ts",
    "preview": "rspack serve --config rspack.config.dev.ts",
    "clean": "rm -rf dist && rm -rf node_modules/.cache",
    "lint": "biome lint src",
    "lint:fix": "biome lint src --write",
    "format": "biome format src",
    "format:fix": "biome format src --write",
    "check": "biome check src",
    "check:fix": "biome check src --write"
  },
  "dependencies": {
    "winbox": "^0.2.82"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@rspack/cli": "^1.7.5",
    "@rspack/core": "^1.7.5",
    "@rspack/plugin-html": "^0.5.8",
    "typescript": "^5.0.0"
  }
}